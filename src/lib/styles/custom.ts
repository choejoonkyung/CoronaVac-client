import { css } from "@emotion/react";

export const resetButton = css`
  border: none;
  outline: none;
  background: none;
  padding: 0;
  cursor: pointer;
`;

export const resetP = css`
  margin: 0;
  padding: 0;
`;
